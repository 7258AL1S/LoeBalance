<Window x:Class="LoeBalance.Desktop.Wpf.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sign In"
        Width="360"
        Height="250"
        ResizeMode="NoResize"
        ShowInTaskbar="True"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontSize="20" FontWeight="SemiBold" Text="LoeBalance" />

        <StackPanel Grid.Row="1" Margin="0,14,0,0">
            <TextBlock FontSize="12" Foreground="#5A6472" Text="Email" />
            <TextBox x:Name="EmailBox" Margin="0,4,0,0" Padding="6" />
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,10,0,0">
            <TextBlock FontSize="12" Foreground="#5A6472" Text="Password" />
            <PasswordBox x:Name="PasswordBox" Margin="0,4,0,0" Padding="6" />
        </StackPanel>

        <TextBlock x:Name="ErrorText"
                   Grid.Row="3"
                   Margin="0,10,0,0"
                   FontSize="12"
                   Foreground="#C62828"
                   TextWrapping="Wrap" />

        <StackPanel Grid.Row="5" HorizontalAlignment="Right" Orientation="Horizontal">
            <TextBlock x:Name="ProgressText"
                       Margin="0,0,12,0"
                       VerticalAlignment="Center"
                       FontSize="12"
                       Foreground="#5A6472" />
            <Button x:Name="SignInButton"
                    Width="96"
                    Height="30"
                    Click="OnSignInClick"
                    Content="Sign In"
                    IsDefault="True" />
        </StackPanel>
    </Grid>
</Window>
